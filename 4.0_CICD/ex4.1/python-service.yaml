apiVersion: v1
kind: Service
metadata:
  name: python-service
  labels:
    app: python
spec:
  selector:
    app: python
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP