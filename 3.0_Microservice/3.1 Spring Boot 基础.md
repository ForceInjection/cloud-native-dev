# Spring Boot 基础教程

Spring Boot 入门基础教程。

## 目录

- [Spring Boot 基础教程](#spring-boot-基础教程)
  - [目录](#目录)
  - [1. 什么是 Spring Boot](#1-什么是-spring-boot)
    - [1.1 简单定义](#11-简单定义)
    - [1.2 类比理解](#12-类比理解)
    - [1.3 发展历程](#13-发展历程)
  - [2. 为什么选择 Spring Boot](#2-为什么选择-spring-boot)
    - [2.1 简化配置](#21-简化配置)
    - [2.2 内嵌服务器](#22-内嵌服务器)
    - [2.3 自动配置](#23-自动配置)
  - [3. 核心概念](#3-核心概念)
    - [3.1 Starter 依赖](#31-starter-依赖)
    - [3.2 自动配置 (Auto Configuration)](#32-自动配置-auto-configuration)
    - [3.3 配置属性 (Configuration Properties)](#33-配置属性-configuration-properties)
  - [4. 项目结构](#4-项目结构)
    - [4.1 标准 Maven 项目结构](#41-标准-maven-项目结构)
    - [4.2 包命名规范](#42-包命名规范)
  - [5. 核心注解](#5-核心注解)
    - [5.1 类级别注解](#51-类级别注解)
      - [5.1.1 `@SpringBootApplication`](#511-springbootapplication)
      - [5.1.2 `@RestController`](#512-restcontroller)
      - [5.1.3 `@Service`](#513-service)
      - [5.1.4 `@Repository`](#514-repository)
    - [5.2 方法级别注解](#52-方法级别注解)
      - [5.2.1 HTTP 映射注解](#521-http-映射注解)
    - [5.3 参数注解](#53-参数注解)
    - [5.4 依赖注入注解](#54-依赖注入注解)
  - [6. 配置管理](#6-配置管理)
    - [6.1 配置文件格式](#61-配置文件格式)
      - [6.1.1 application.properties](#611-applicationproperties)
      - [6.1.2 application.yml (推荐)](#612-applicationyml-推荐)
    - [6.2 多环境配置](#62-多环境配置)
    - [6.3 自定义配置属性](#63-自定义配置属性)
  - [7. 依赖注入](#7-依赖注入)
    - [7.1 什么是依赖注入？](#71-什么是依赖注入)
    - [7.2 注入方式对比](#72-注入方式对比)
      - [7.2.1 构造器注入（推荐）](#721-构造器注入推荐)
      - [7.2.2 字段注入（不推荐）](#722-字段注入不推荐)
    - [7.3 Bean 的作用域](#73-bean-的作用域)
  - [8. Web 开发](#8-web-开发)
    - [8.1 RESTful API 设计](#81-restful-api-设计)
    - [8.2 数据验证](#82-数据验证)
    - [8.3 异常处理](#83-异常处理)
  - [9. 数据访问](#9-数据访问)
    - [9.1 JPA 实体类](#91-jpa-实体类)
    - [9.2 Repository 接口](#92-repository-接口)
    - [9.3 Service 层](#93-service-层)
  - [10. 微服务简介](#10-微服务简介)
    - [10.1 微服务架构概念](#101-微服务架构概念)
    - [10.2 Spring Cloud 简介](#102-spring-cloud-简介)
    - [10.3 基本示例](#103-基本示例)
    - [10.4 实战练习](#104-实战练习)
  - [11. 最佳实践](#11-最佳实践)
    - [11.1 项目结构最佳实践](#111-项目结构最佳实践)
    - [11.2 代码规范](#112-代码规范)
      - [11.2.1 命名规范](#1121-命名规范)
      - [11.2.2 注释规范](#1122-注释规范)
    - [11.3 性能优化](#113-性能优化)
      - [11.3.1 数据库优化](#1131-数据库优化)
      - [11.3.2 异步处理](#1132-异步处理)
    - [11.4 安全最佳实践](#114-安全最佳实践)
    - [11.5 测试最佳实践](#115-测试最佳实践)
  - [12. 总结](#12-总结)
    - [12.1 核心特性](#121-核心特性)
    - [12.2 推荐资源](#122-推荐资源)

---

## 1. 什么是 Spring Boot

### 1.1 简单定义

Spring Boot 是一个基于 Spring 框架的**快速开发工具**，它的核心理念是：

- **约定优于配置** (Convention over Configuration)
- **开箱即用** (Out of the box)
- **快速启动** (Quick Start)

### 1.2 类比理解

想象一下：

- **传统 Spring** = 自己搭建房子（需要准备砖头、水泥、钢筋...）
- **Spring Boot** = 精装修房子（拎包入住，基础设施都配好了）

### 1.3 发展历程

```text
2003年 → Spring Framework 诞生
2014年 → Spring Boot 1.0 发布
2018年 → Spring Boot 2.0 发布（重大更新）
2023年 → Spring Boot 3.0 发布（支持 Java 17+）
```

---

## 2. 为什么选择 Spring Boot

### 2.1 简化配置

**传统 Spring 项目**：

```xml
<!-- 需要大量 XML 配置 -->
<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource">
    <property name="driverClassName" value="com.mysql.jdbc.Driver"/>
    <property name="url" value="jdbc:mysql://localhost:3306/test"/>
    <!-- 更多配置... -->
</bean>
```

**Spring Boot 项目**：

```properties
# 只需要简单的属性配置
spring.datasource.url=jdbc:mysql://localhost:3306/test
spring.datasource.username=root
spring.datasource.password=123456
```

### 2.2 内嵌服务器

- **传统方式**：需要单独安装 Tomcat、配置部署
- **Spring Boot**：内嵌 Tomcat，直接运行 `java -jar app.jar`

### 2.3 自动配置

Spring Boot 会根据你添加的依赖自动配置相应的功能：

```java
// 只要添加了 spring-boot-starter-web 依赖
// Spring Boot 就会自动配置：
// - DispatcherServlet
// - 视图解析器
// - 错误页面
// - JSON 转换器
// 等等...
```

---

## 3. 核心概念

### 3.1 Starter 依赖

**什么是 Starter？**

Starter 是一组预定义的依赖集合，就像"套餐"一样。

```xml
<!-- Web 开发套餐 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>

<!-- 数据库访问套餐 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-jpa</artifactId>
</dependency>
```

**常用 Starter**：

| Starter | 功能 | 包含的主要依赖 |
|---------|------|----------------|
| `spring-boot-starter-web` | Web 开发 | Spring MVC, Tomcat, Jackson |
| `spring-boot-starter-data-jpa` | JPA 数据访问 | Hibernate, Spring Data JPA |
| `spring-boot-starter-security` | 安全框架 | Spring Security |
| `spring-boot-starter-test` | 测试 | JUnit, Mockito, Spring Test |

### 3.2 自动配置 (Auto Configuration)

**工作原理**：

```java
@SpringBootApplication
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

`@SpringBootApplication` 包含三个重要注解：

```java
@SpringBootConfiguration  // 标识为配置类
@EnableAutoConfiguration  // 启用自动配置
@ComponentScan           // 组件扫描
```

**自动配置示例**：

```java
// 当检测到 DataSource 相关的类在 classpath 中时
// Spring Boot 会自动创建 DataSource Bean
@ConditionalOnClass(DataSource.class)
@AutoConfigureAfter(DataSourceAutoConfiguration.class)
public class JpaRepositoriesAutoConfiguration {
    // 自动配置 JPA 相关的 Bean
}
```

### 3.3 配置属性 (Configuration Properties)

**外部化配置**：

```properties
# application.properties
server.port=8080
spring.datasource.url=jdbc:mysql://localhost:3306/test
spring.jpa.hibernate.ddl-auto=update
```

**配置优先级**（从高到低）：

1. 命令行参数：`java -jar app.jar --server.port=9090`
2. 系统环境变量
3. `application.properties` 文件
4. 默认值

---

## 4. 项目结构

### 4.1 标准 Maven 项目结构

```text
my-spring-boot-app/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/example/demo/
│   │   │       ├── DemoApplication.java     # 启动类
│   │   │       ├── controller/              # 控制器层
│   │   │       ├── service/                 # 服务层
│   │   │       ├── repository/              # 数据访问层
│   │   │       └── domain/                  # 实体类
│   │   └── resources/
│   │       ├── application.properties       # 配置文件
│   │       ├── static/                      # 静态资源
│   │       └── templates/                   # 模板文件
│   └── test/
│       └── java/                            # 测试代码
├── target/                                  # 编译输出
└── pom.xml                                  # Maven 配置
```

### 4.2 包命名规范

```java
// 推荐的包结构
com.company.project
├── Application.java           # 主启动类
├── config/                   # 配置类
├── controller/               # REST 控制器
├── service/                  # 业务逻辑
│   └── impl/                # 业务逻辑实现
├── repository/               # 数据访问
├── domain/                   # 实体类
├── dto/                      # 数据传输对象
└── exception/                # 异常处理
```

---

## 5. 核心注解

### 5.1 类级别注解

#### 5.1.1 `@SpringBootApplication`

```java
@SpringBootApplication
public class DemoApplication {
    public static void main(String[] args) {
        SpringApplication.run(DemoApplication.class, args);
    }
}
```

**作用**：标识主配置类，启用自动配置和组件扫描

#### 5.1.2 `@RestController`

```java
@RestController
public class UserController {
    
    @GetMapping("/users")
    public List<User> getAllUsers() {
        return userService.findAll();
    }
}
```

**作用**：`@Controller` + `@ResponseBody`，返回 JSON 数据

#### 5.1.3 `@Service`

```java
@Service
public class UserService {
    
    @Autowired
    private UserRepository userRepository;
    
    public User findById(Long id) {
        return userRepository.findById(id).orElse(null);
    }
}
```

**作用**：标识服务层组件

#### 5.1.4 `@Repository`

```java
@Repository
public interface UserRepository extends JpaRepository<User, Long> {
    List<User> findByName(String name);
}
```

**作用**：标识数据访问层组件

### 5.2 方法级别注解

#### 5.2.1 HTTP 映射注解

```java
@RestController
@RequestMapping("/api/users")
public class UserController {
    
    @GetMapping                    // GET /api/users
    public List<User> getUsers() { ... }
    
    @GetMapping("/{id}")          // GET /api/users/1
    public User getUser(@PathVariable Long id) { ... }
    
    @PostMapping                   // POST /api/users
    public User createUser(@RequestBody User user) { ... }
    
    @PutMapping("/{id}")          // PUT /api/users/1
    public User updateUser(@PathVariable Long id, @RequestBody User user) { ... }
    
    @DeleteMapping("/{id}")       // DELETE /api/users/1
    public void deleteUser(@PathVariable Long id) { ... }
}
```

### 5.3 参数注解

```java
@GetMapping("/search")
public List<User> searchUsers(
    @RequestParam String name,              // ?name=张三
    @RequestParam(defaultValue = "0") int page,  // ?page=1
    @RequestHeader("User-Agent") String userAgent  // HTTP 头
) {
    return userService.search(name, page);
}
```

### 5.4 依赖注入注解

```java
@Service
public class UserService {
    
    // 字段注入（不推荐）
    @Autowired
    private UserRepository userRepository;
    
    // 构造器注入（推荐）
    private final UserRepository userRepository;
    
    public UserService(UserRepository userRepository) {
        this.userRepository = userRepository;
    }
}
```

---

## 6. 配置管理

### 6.1 配置文件格式

#### 6.1.1 application.properties

```properties
# 服务器配置
server.port=8080
server.servlet.context-path=/api

# 数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/demo
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA 配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
```

#### 6.1.2 application.yml (推荐)

```yaml
server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
```

### 6.2 多环境配置

```text
application.yml              # 通用配置
application-dev.yml          # 开发环境
application-test.yml         # 测试环境
application-prod.yml         # 生产环境
```

**激活特定环境**：

```yaml
# application.yml
spring:
  profiles:
    active: dev  # 激活开发环境配置
```

或者通过命令行：

```bash
java -jar app.jar --spring.profiles.active=prod
```

### 6.3 自定义配置属性

```java
@ConfigurationProperties(prefix = "app")
@Component
public class AppProperties {
    
    private String name;
    private String version;
    private Database database = new Database();
    
    // getter 和 setter
    
    public static class Database {
        private String host;
        private int port;
        // getter 和 setter
    }
}
```

```yaml
# application.yml
app:
  name: My Application
  version: 1.0.0
  database:
    host: localhost
    port: 3306
```

---

## 7. 依赖注入

### 7.1 什么是依赖注入？

**传统方式**（紧耦合）：

```java
public class UserService {
    private UserRepository userRepository = new UserRepository(); // 硬编码依赖
    
    public User findById(Long id) {
        return userRepository.findById(id);
    }
}
```

**依赖注入**（松耦合）：

```java
@Service
public class UserService {
    private final UserRepository userRepository;
    
    // Spring 会自动注入 UserRepository 的实现
    public UserService(UserRepository userRepository) {
        this.userRepository = userRepository;
    }
    
    public User findById(Long id) {
        return userRepository.findById(id);
    }
}
```

### 7.2 注入方式对比

#### 7.2.1 构造器注入（推荐）

```java
@Service
public class UserService {
    private final UserRepository userRepository;
    private final EmailService emailService;
    
    public UserService(UserRepository userRepository, EmailService emailService) {
        this.userRepository = userRepository;
        this.emailService = emailService;
    }
}
```

**优点**：

- 保证依赖不为 null
- 支持 final 字段
- 便于单元测试
- 循环依赖检测

#### 7.2.2 字段注入（不推荐）

```java
@Service
public class UserService {
    @Autowired
    private UserRepository userRepository;
    
    @Autowired
    private EmailService emailService;
}
```

**缺点**：

- 难以进行单元测试
- 隐藏依赖关系
- 可能出现 null 值

### 7.3 Bean 的作用域

```java
@Service
@Scope("singleton")    // 默认，单例
public class UserService { ... }

@Service
@Scope("prototype")    // 每次注入都创建新实例
public class TaskService { ... }

@Service
@Scope("request")      // Web 环境，每个 HTTP 请求一个实例
public class RequestService { ... }
```

---

## 8. Web 开发

### 8.1 RESTful API 设计

```java
@RestController
@RequestMapping("/api/users")
public class UserController {
    
    private final UserService userService;
    
    public UserController(UserService userService) {
        this.userService = userService;
    }
    
    // 获取所有用户
    @GetMapping
    public ResponseEntity<List<User>> getAllUsers() {
        List<User> users = userService.findAll();
        return ResponseEntity.ok(users);
    }
    
    // 根据 ID 获取用户
    @GetMapping("/{id}")
    public ResponseEntity<User> getUserById(@PathVariable Long id) {
        User user = userService.findById(id);
        if (user != null) {
            return ResponseEntity.ok(user);
        } else {
            return ResponseEntity.notFound().build();
        }
    }
    
    // 创建用户
    @PostMapping
    public ResponseEntity<User> createUser(@Valid @RequestBody User user) {
        User savedUser = userService.save(user);
        return ResponseEntity.status(HttpStatus.CREATED).body(savedUser);
    }
    
    // 更新用户
    @PutMapping("/{id}")
    public ResponseEntity<User> updateUser(
            @PathVariable Long id, 
            @Valid @RequestBody User user) {
        User updatedUser = userService.update(id, user);
        return ResponseEntity.ok(updatedUser);
    }
    
    // 删除用户
    @DeleteMapping("/{id}")
    public ResponseEntity<Void> deleteUser(@PathVariable Long id) {
        userService.deleteById(id);
        return ResponseEntity.noContent().build();
    }
}
```

### 8.2 数据验证

```java
@Entity
public class User {
    
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @NotBlank(message = "用户名不能为空")
    @Size(min = 2, max = 50, message = "用户名长度必须在2-50之间")
    private String name;
    
    @Email(message = "邮箱格式不正确")
    @NotBlank(message = "邮箱不能为空")
    private String email;
    
    @Min(value = 18, message = "年龄不能小于18岁")
    @Max(value = 100, message = "年龄不能大于100岁")
    private Integer age;
    
    // getter 和 setter
}
```

### 8.3 异常处理

```java
@ControllerAdvice
public class GlobalExceptionHandler {
    
    // 处理数据验证异常
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<Map<String, String>> handleValidationExceptions(
            MethodArgumentNotValidException ex) {
        Map<String, String> errors = new HashMap<>();
        ex.getBindingResult().getAllErrors().forEach((error) -> {
            String fieldName = ((FieldError) error).getField();
            String errorMessage = error.getDefaultMessage();
            errors.put(fieldName, errorMessage);
        });
        return ResponseEntity.badRequest().body(errors);
    }
    
    // 处理资源未找到异常
    @ExceptionHandler(ResourceNotFoundException.class)
    public ResponseEntity<String> handleResourceNotFound(ResourceNotFoundException ex) {
        return ResponseEntity.notFound().build();
    }
    
    // 处理通用异常
    @ExceptionHandler(Exception.class)
    public ResponseEntity<String> handleGenericException(Exception ex) {
        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)
                .body("服务器内部错误：" + ex.getMessage());
    }
}
```

---

## 9. 数据访问

### 9.1 JPA 实体类

```java
@Entity
@Table(name = "users")
public class User {
    
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(name = "username", nullable = false, unique = true)
    private String name;
    
    @Column(name = "email", nullable = false)
    private String email;
    
    @Column(name = "created_at")
    @CreationTimestamp
    private LocalDateTime createdAt;
    
    @Column(name = "updated_at")
    @UpdateTimestamp
    private LocalDateTime updatedAt;
    
    // 构造器、getter 和 setter
    public User() {}
    
    public User(String name, String email) {
        this.name = name;
        this.email = email;
    }
    
    // getter 和 setter 方法...
}
```

### 9.2 Repository 接口

```java
@Repository
public interface UserRepository extends JpaRepository<User, Long> {
    
    // 根据名称查找用户
    List<User> findByName(String name);
    
    // 根据邮箱查找用户
    Optional<User> findByEmail(String email);
    
    // 根据名称模糊查询
    List<User> findByNameContaining(String name);
    
    // 自定义查询
    @Query("SELECT u FROM User u WHERE u.email = ?1")
    User findUserByEmail(String email);
    
    // 原生 SQL 查询
    @Query(value = "SELECT * FROM users WHERE name LIKE %?1%", nativeQuery = true)
    List<User> findByNameLike(String name);
    
    // 更新操作
    @Modifying
    @Query("UPDATE User u SET u.email = ?2 WHERE u.id = ?1")
    int updateUserEmail(Long id, String email);
}
```

### 9.3 Service 层

```java
@Service
@Transactional
public class UserService {
    
    private final UserRepository userRepository;
    
    public UserService(UserRepository userRepository) {
        this.userRepository = userRepository;
    }
    
    @Transactional(readOnly = true)
    public List<User> findAll() {
        return userRepository.findAll();
    }
    
    @Transactional(readOnly = true)
    public User findById(Long id) {
        return userRepository.findById(id)
                .orElseThrow(() -> new ResourceNotFoundException("用户不存在，ID: " + id));
    }
    
    public User save(User user) {
        // 业务逻辑验证
        if (userRepository.findByEmail(user.getEmail()).isPresent()) {
            throw new IllegalArgumentException("邮箱已存在");
        }
        return userRepository.save(user);
    }
    
    public User update(Long id, User userDetails) {
        User user = findById(id);
        user.setName(userDetails.getName());
        user.setEmail(userDetails.getEmail());
        return userRepository.save(user);
    }
    
    public void deleteById(Long id) {
        if (!userRepository.existsById(id)) {
            throw new ResourceNotFoundException("用户不存在，ID: " + id);
        }
        userRepository.deleteById(id);
    }
}
```

---

## 10. 微服务简介

### 10.1 微服务架构概念

**单体应用 vs 微服务**：

```text
单体应用：一个大应用包含所有功能模块
微服务：将应用拆分为多个独立的小服务
```

**微服务的优势**：

- 独立部署和扩展
- 技术栈多样化
- 团队独立开发
- 故障隔离

### 10.2 Spring Cloud 简介

Spring Cloud 是构建微服务的工具集，主要组件包括：

- **Eureka**：服务注册与发现
- **OpenFeign**：声明式服务调用
- **Gateway**：API 网关
- **Config**：配置中心

### 10.3 基本示例

**服务注册**：

```java
@SpringBootApplication
@EnableEurekaClient
public class UserServiceApplication {
    // 启动类
}
```

**服务调用**：

```java
@FeignClient(name = "user-service")
public interface UserFeign {
    @GetMapping("/users/{id}")
    User getUserById(@PathVariable Long id);
}
```

### 10.4 实战练习

本章的微服务示例可以在项目的 `ex3.2` 目录中找到，包含：

- Eureka 服务注册中心
- 用户服务（User Service）
- 管理服务（Admin Service）

详细的微服务架构设计和实现请参考下一章《使用 Spring Cloud 构建微服务》。

---

## 11. 最佳实践

### 11.1 项目结构最佳实践

```java
// 推荐的分层架构
com.example.userservice
├── UserServiceApplication.java    # 启动类
├── config/                        # 配置类
│   ├── DatabaseConfig.java
│   └── SwaggerConfig.java
├── controller/                    # 控制器层
│   └── UserController.java
├── service/                       # 服务层
│   ├── UserService.java
│   └── impl/
│       └── UserServiceImpl.java
├── repository/                    # 数据访问层
│   └── UserRepository.java
├── domain/                        # 实体类
│   └── User.java
├── dto/                          # 数据传输对象
│   ├── UserDto.java
│   └── UserVO.java
└── exception/                    # 异常处理
    ├── GlobalExceptionHandler.java
    └── ResourceNotFoundException.java
```

### 11.2 代码规范

#### 11.2.1 命名规范

```java
// 类名：大驼峰命名法
public class UserService { }

// 方法名：小驼峰命名法
public User findUserById(Long id) { }

// 常量：全大写，下划线分隔
public static final String DEFAULT_PASSWORD = "123456";

// 包名：全小写，点分隔
com.example.userservice.controller
```

#### 11.2.2 注释规范

```java
/**
 * 用户服务类
 * 
 * @author 张三
 * @since 1.0.0
 */
@Service
public class UserService {
    
    /**
     * 根据用户ID查找用户
     * 
     * @param id 用户ID
     * @return 用户信息
     * @throws ResourceNotFoundException 当用户不存在时抛出
     */
    public User findById(Long id) {
        return userRepository.findById(id)
                .orElseThrow(() -> new ResourceNotFoundException("用户不存在"));
    }
}
```

### 11.3 性能优化

#### 11.3.1 数据库优化

```java
// 使用分页查询
@GetMapping("/users")
public Page<User> getUsers(
        @RequestParam(defaultValue = "0") int page,
        @RequestParam(defaultValue = "10") int size) {
    Pageable pageable = PageRequest.of(page, size);
    return userService.findAll(pageable);
}

// 使用缓存
@Service
@EnableCaching
public class UserService {
    
    @Cacheable(value = "users", key = "#id")
    public User findById(Long id) {
        return userRepository.findById(id).orElse(null);
    }
    
    @CacheEvict(value = "users", key = "#user.id")
    public User update(User user) {
        return userRepository.save(user);
    }
}
```

#### 11.3.2 异步处理

```java
@Service
public class EmailService {
    
    @Async
    public CompletableFuture<Void> sendEmail(String to, String subject, String content) {
        // 发送邮件的逻辑
        return CompletableFuture.completedFuture(null);
    }
}

@Configuration
@EnableAsync
public class AsyncConfig {
    
    @Bean
    public TaskExecutor taskExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(2);
        executor.setMaxPoolSize(5);
        executor.setQueueCapacity(100);
        executor.setThreadNamePrefix("async-");
        executor.initialize();
        return executor;
    }
}
```

### 11.4 安全最佳实践

```java
// 输入验证
@PostMapping("/users")
public ResponseEntity<User> createUser(@Valid @RequestBody CreateUserRequest request) {
    User user = userService.createUser(request);
    return ResponseEntity.status(HttpStatus.CREATED).body(user);
}

// 密码加密
@Service
public class UserService {
    
    @Autowired
    private PasswordEncoder passwordEncoder;
    
    public User createUser(CreateUserRequest request) {
        User user = new User();
        user.setUsername(request.getUsername());
        user.setPassword(passwordEncoder.encode(request.getPassword()));
        return userRepository.save(user);
    }
}

// 配置密码编码器
@Configuration
public class SecurityConfig {
    
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

### 11.5 测试最佳实践

```java
// 单元测试
@ExtendWith(MockitoExtension.class)
class UserServiceTest {
    
    @Mock
    private UserRepository userRepository;
    
    @InjectMocks
    private UserService userService;
    
    @Test
    void shouldReturnUserWhenValidId() {
        // Given
        Long userId = 1L;
        User expectedUser = new User("张三", "zhangsan@example.com");
        when(userRepository.findById(userId)).thenReturn(Optional.of(expectedUser));
        
        // When
        User actualUser = userService.findById(userId);
        
        // Then
        assertThat(actualUser).isEqualTo(expectedUser);
    }
}

// 集成测试
@SpringBootTest
@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
@Testcontainers
class UserControllerIntegrationTest {
    
    @Container
    static MySQLContainer<?> mysql = new MySQLContainer<>("mysql:8.0")
            .withDatabaseName("testdb")
            .withUsername("test")
            .withPassword("test");
    
    @Autowired
    private TestRestTemplate restTemplate;
    
    @Test
    void shouldCreateUserSuccessfully() {
        // Given
        CreateUserRequest request = new CreateUserRequest("张三", "zhangsan@example.com");
        
        // When
        ResponseEntity<User> response = restTemplate.postForEntity(
                "/api/users", request, User.class);
        
        // Then
        assertThat(response.getStatusCode()).isEqualTo(HttpStatus.CREATED);
        assertThat(response.getBody().getName()).isEqualTo("张三");
    }
}
```

---

## 12. 总结

Spring Boot 作为现代 Java 开发的主流框架，具有以下核心优势：

### 12.1 核心特性

1. **约定优于配置** - 减少样板代码
2. **自动配置** - 智能的默认配置
3. **内嵌服务器** - 简化部署流程
4. **生产就绪** - 内置监控和管理功能

### 12.2 推荐资源

- **官方文档**: [Spring Boot Reference Guide](https://docs.spring.io/spring-boot/docs/current/reference/htmlsingle/)
- **官方指南**: [Spring Guides](https://spring.io/guides)
- **示例项目**: [Spring Boot Samples](https://github.com/spring-projects/spring-boot/tree/main/spring-boot-samples)
- **社区资源**: [Spring Boot 中文社区](https://springboot.io/)

---
